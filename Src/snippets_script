; snippets_assemble_script
;
;
; line 1  assembles snippets.s (text) to snippets (program) using VASM
;         (with symbols and linedebug information included in program)
;
; line 2  start the debugger and loads the program and the text.
;
;
//VASM/vasmm68k_mot -Fhunkexe -kick1hunks -m68080 -linedebug -quiet -o snippets snippets.s 
/monam snippets
